/**
 * Features Split Types (Why Us — 2-column: features left, image right)
 */

export interface FeaturesSplitContent {
  tagline: string
  title: string
  description: string
  features: string[]
  cta: { text: string }
}
